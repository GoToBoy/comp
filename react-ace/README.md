library: react-ace ace-builds
install: npm install react-ace ace-builds
desc: support freemarker syntax highlighting snippet and auto-complete.

usesage:

```javascript
<MyAceEditor defaultValue="defaultValue" onChange={(value) => {}} />
```
